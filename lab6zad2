#include <stdio.h>
#include <stdlib.h>

int swap(int *a, int *b)
{
    int wsk = * a;
    * a = * b;
    * b = wsk;

    return 0;
}

int main()
{
    int a=5;
    int b=1;

    swap(&a,&b);
    printf("a=%d b=%d",a,b);
    return 0;
}
